{
  "name": null,
  "project": "praxis",
  "category": "feature",
  "branchName": "chief/72-deeper-agora-integration",
  "baseBranch": "main",
  "description": "Praxis's LLM client (`praxis/llm.py`) is BYO-key with an OPTIONAL agora path: when `AGORA_BASE_URL` is set every call routes through agora's provider-router instead of the provider's own endpoint (`LLMConfig.routed_via_agora` / `.endpoint` / `.wire_format`), and unset means go direct. This tasklist deepens that opt-in — richer provider-router use behind `AGORA_BASE_URL` (e.g. surfacing router-reported model/route info, honoring router selection/fallback, cleaner error surfacing from the router) — **without ever becoming a hard dependency**: with `AGORA_BASE_URL` unset the direct BYO-key path is byte-for-byte what ships, and no import or call assumes agora is reachable. Because it stays optional, this has NO hard `dependsOn` on agora; it consumes agora's contract by reference (koine specifies, agora implements) rather than re-specifying it. Scope: `praxis/llm.py` (config + call path), the construct/checks callers only where they surface a route, and `tests/` (agora on vs off, both pinned with a stub — no live agora).",
  "parked": false,
  "dependsOn": [],
  "touches": [
    "praxis/",
    "tests/"
  ],
  "iters": 6,
  "warmup": [
    "echo 'agora is opt-in behind AGORA_BASE_URL and must NEVER become a hard dependency: unset -> direct BYO-key path unchanged. Consume the contract by reference, do not re-specify it — see praxis/llm.py header and ../CLAUDE.md.'",
    "sed -n '1,40p' praxis/llm.py",
    "grep -n 'AGORA_BASE_URL\\|routed_via_agora\\|def endpoint\\|def wire_format\\|def describe' praxis/llm.py"
  ],
  "userStories": [
    {
      "id": "US-1",
      "title": "Richer provider-router use behind AGORA_BASE_URL",
      "acceptanceCriteria": [
        "With `AGORA_BASE_URL` set, `praxis/llm.py` uses agora's provider-router more richly than a plain base-URL swap — e.g. it honors the router's selected model/route in what it records and surfaces the router's own error body (the way the client already prefers the provider's message) — extending `LLMConfig`/the call path, not adding a second client.",
        "The agora path stays authenticated per the client's existing rule (`AGORA_API_KEY`, falling back to the provider key) and sends no sampling params the models reject; `describe()` reflects the deeper routing so a human can see the route in use.",
        "Gate green: `python3 -m pytest -q tests/` passes, with a test that drives the agora-routed path against a stub endpoint (no live agora) asserting the request goes to the router and the router's response/errors are surfaced."
      ],
      "passes": true,
      "notes": "praxis/llm.py: AGORA_ROUTE/AGORA_FALLBACK_MODELS ride in x-agora-* headers (never the body, so no param a provider rejects); the router's served model/provider/route/request-id are read back into LLMClient.last_route (falls back to the reply's model/id when the router is quiet); a router HTTPError is reported as the router's with error.message extracted, and an unreachable router names AGORA_BASE_URL as the way back to direct. describe() now shows route + fallback. Observed: .venv/bin/python -m pytest -q tests/ = 802 passed, 1 warning (30 in tests/test_llm.py, 10 of them new); doctor prints 'anthropic/claude-opus-5 via agora provider-router at http://localhost:9000/v1/chat/completions route fast fallback gpt-4o (timeout 120s)' with AGORA_BASE_URL set and the unchanged '... via direct at https://api.anthropic.com/v1/messages (timeout 120s)' without it."
    },
    {
      "id": "US-2",
      "title": "Opt-in only — unset AGORA_BASE_URL is the shipped direct path, untouched",
      "acceptanceCriteria": [
        "With `AGORA_BASE_URL` unset, the direct BYO-key path is unchanged from what ships — same endpoint resolution, wire format and auth per provider — and no module imports or call assumes agora is present or reachable; agora is never a hard dependency of any code path.",
        "A test pins both modes from the same config resolution (agora on vs off) using an injected env/stub, so the direct path is proven untouched by the deeper integration, and no `dependsOn` on agora is introduced (the contract is consumed by reference).",
        "Gate green: `python3 -m pytest -q tests/` passes; `npm run build` in ui/ and `cargo build` in src-tauri/ pass if either is touched, otherwise unaffected."
      ],
      "passes": true,
      "notes": "tests/test_llm.py grew a pinning section (9 tests): DIRECT_WIRE asserts the whole direct request per provider (url, exact header dict, payload, timeout, describe()) and the direct 429/URLError strings verbatim; one env resolves agora-off vs agora-on side by side (same provider/model/key/timeout, only endpoint+wire_format+headers differ); AGORA_ROUTE/AGORA_API_KEY/AGORA_FALLBACK_MODELS with AGORA_BASE_URL unset give a call byte-identical to the same env without them; building a client in either mode makes zero requests; no module imports agora and pyproject/ui-package.json/Cargo.toml name it nowhere (no dependsOn added). Separately verified untouched vs main: drove `git show main:praxis/llm.py` and HEAD's through the same stub over 6 direct configs + 4 config-error cases comparing endpoint/wire/key/headers/payload/text/describe and all four error strings -> DIFFS: 0. Observed: .venv/bin/python -m pytest -q tests/ = 812 passed, 1 warning (40 in tests/test_llm.py); node scripts/check-doc-links.mjs --ratchet --base main = 0 dead vs 0 on main. ui/ and src-tauri/ untouched."
    }
  ],
  "mergedToMain": "e005aad"
}
