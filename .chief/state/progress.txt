# Chief Progress — 84-nbgrader-cell-schema-adoption
Started: Fri Aug 21 19:10:28 CDT 2026
---

## Codebase Patterns

- `uv run --extra dev` is the reproducible project environment; it creates `.venv` with the pinned `nbgrader==0.9.5` dependency used by the validation gate.
- The branch's `.chief/state/prd.json` is the runtime source of truth; the tracked tasklist mirror must be kept in sync when it exists.

## Verification recovery

The previous verification failure was caused by the local environment lacking the pinned nbgrader executable, which made all 24 converted notebooks unavailable to the gate and caused construction checks to fail. Recreated the project environment with `uv run --extra dev`, then rebased the branch onto `main` and resolved the tasklist/progress conflicts while retaining this branch's nbgrader stories.

Observed verification after recovery:
- `uv run --extra dev python scripts/validate_nbgrader.py notebooks`: `nbgrader validate: 24 notebooks, 0 failures`.
- `uv run --extra dev pytest -q tests/`: `909 passed, 1 skipped`.
- `CHIEF_BASE_BRANCH=main .chief/verify.sh`: `doc-links: 0 dead vs 0 on main`, nbgrader validation 24/24 with 0 failures, `909 passed, 1 skipped`, `verify: PASS`.

Learnings for future iterations: a missing `.venv` can masquerade as widespread nbgrader and constructor regressions; install from the locked project environment before debugging application failures. Rebase conflicts in ignored Chief state files require force-adding the resolved runtime/task progress files during the rebase.
