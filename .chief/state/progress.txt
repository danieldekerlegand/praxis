# Chief Progress — 84-nbgrader-cell-schema-adoption
Started: Fri Aug 21 19:20:32 CDT 2026
---

## Codebase Patterns

- `uv sync --extra dev` creates the repository-local `.venv` with the pinned `nbgrader==0.9.5` dependency and editable Praxis install; use that environment for the Python gate.
- `.chief/verify.sh` selects `.venv/bin/python` when present, runs `scripts/validate_nbgrader.py` before the full pytest suite, and uses `CHIEF_BASE_BRANCH=main` for branch verification.
- The runtime PRD is the source of truth; the matching `tasks/chief/84-nbgrader-cell-schema-adoption.json` mirror must remain synchronized when it exists.

## Verification recovery

The previous verification run was invalid because this fresh worktree did not yet have `.venv`; without the pinned nbgrader install, all 24 converted notebooks were reported unavailable and dependent tests failed. Recreated the environment with `uv sync --extra dev`.

Observed verification after recovery:

- `CHIEF_BASE_BRANCH=main .chief/verify.sh`: PASS
- `scripts/validate_nbgrader.py notebooks`: 24 notebooks, 0 failures
- `python -m pytest -q tests/`: 918 passed, 1 skipped
- Documentation links: 0 dead vs 0 on main

## Learnings for future iterations

A clean worktree may omit the ignored repository environment even when the project is correctly configured. Install from the project manifest before diagnosing nbgrader-dependent failures; the failure was environmental, not a regression in the already-completed stories.
