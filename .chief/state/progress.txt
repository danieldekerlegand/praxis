# Chief Progress — 83-seed-library-refresh
Started: Fri Aug 21 04:14:07 CDT 2026
---

## Codebase Patterns

- Repo-maintenance notebook operations should compose `praxis.audit` and `praxis.construct`; the constructor is the only writer and its pre-write `construction_failures` check is the anti-fabrication barrier.

## Iteration US-2

Implemented the seed refresh policy in `docs/explanation/seed-refresh-policy.md` and added `praxis.refresh` with explicit `FLAGGED-FOR-HUMAN` default and per-notebook `FORCE-REGENERATE` opt-in. The maintenance CLI/API audits first, refuses to target healthy complete seeds, and delegates forced writes to the shipped constructor. Added tests for default read-only flagging, failed forced regeneration preserving the original bytes, and healthy-seed refusal.

Verification observed: `python3 -m pytest -q` — 914 passed, 1 skipped; focused refresh tests — 6 passed.

Learnings for future iterations: `construct_path` remains the single repo writer; refresh code should stay a policy layer around it rather than adding another notebook-generation or grading path.
