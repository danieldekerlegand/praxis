# Chief Progress — 901-docs-tell-the-truth
Started: Thu Sep  3 12:01:48 CDT 2026
---

## Codebase Patterns

- **The docs gates are a PAIR, and they live in three places that must stay in step:**
  `scripts/check-doc-links.mjs` (local references resolve — a **ratchet** vs the base) and
  `scripts/check-docs-structure.mjs` (index coverage · banners · closed directory set — a
  **wall**). Both are wired into `.chief/verify.sh` *and* `.github/workflows/ci.yml`.
  CLAUDE.md's "change one and change the other" rule applies: verify.sh + ci.yml + the
  guard's own header comment.
- **`chief verify` runs the MAIN CHECKOUT's `.chief/verify.sh`, not the worktree's.**
  Observed 2026-09-03: the header line reads
  `>> chief verify: /Users/dldekerlegand/Development/praxis/.chief/verify.sh (cwd=<worktree>)`.
  So a NEW step added to `.chief/verify.sh` inside a worktree is **not exercised by
  `chief verify` on that branch** — it only takes effect after the merge. Run the new
  check directly and say so in the story notes.
- **NEVER `git checkout .chief/state/prd.json`.** It is deliberately *uncommitted* runtime
  state; HEAD's copy is the PREVIOUS tasklist's finished PRD (all stories `passes: true`).
  I did this and clobbered 901's PRD with 900's. Recovery, should it happen again: the
  git-tracked `tasks/chief/<name>.json` is a byte-identical copy of the runtime PRD as
  installed, so `cp tasks/chief/<name>.json .chief/state/prd.json` restores it.
- **Backtick every `docs/...` path you name in a story's `notes`.**
  `scripts/check-doc-links.mjs` scans tracked JSON for bare `docs/...md` paths, and
  `tasks/chief/<name>.json` IS tracked — so a note describing a file you deliberately
  moved away from, or a violation you injected to prove a gate fires, registers as a
  doc-link REGRESSION and fails the merge. Observed: 3 dead references, all of them
  prose in my own notes. Backticks suppress it (the scanner's negative lookbehind).
- **Write JSON back with `ensure_ascii=False`.** Both the PRD and the tasklist are full of
  `—` and `·`; `json.dumps` defaults to escaping them and turns a 2-line edit into a
  whole-file diff.
- The documentation standard's home of record is
  `/Users/dldekerlegand/Development/rosetta/docs/reference/documentation-standard.md`
  (private repo — cite as backticked prose, never a relative link). Tier 1 root files:
  `README.md` `CLAUDE.md` `ROADMAP.md` `CHANGELOG.md` `LICENSE`, nothing else. Seven
  `docs/` dirs, a ceiling not a quota. R1 index · R2 banner · R3 archive-never-delete.

---

## US-1 — Every document is reachable, banner-stamped, and in the right place  (commit d711aff)

**Measured first.** 4 of 13 `docs/` files had no banner, not the tasklist's 3 —
`docs/explanation/dead-code-inventory.md` arrived from `900-dead-code-paydown` after that
measurement was taken. 3 were unreachable from `docs/README.md` (dead-code-inventory,
domain-addition-policy, seed-refresh-policy). 1 dead internal link. 0 undeclared
directories under `docs/`.

**Files changed**
- `docs/explanation/{dead-code-inventory,domain-addition-policy,gating-backfill-cost,seed-refresh-policy}.md`
  — banner added, dated to each file's last content change (`git log -1 --date=short`),
  not to today. A banner claiming today's date on a doc nobody touched is the lie the
  banner exists to prevent.
- `docs/README.md` — rewritten: all 13 subdocs linked; the citation of the standard changed
  from a relative path into private `rosetta` to backticked prose; a new "shape of this
  tree" section stating in writing that 0 directories fall outside the seven and why the
  two generated docs must not be hand-edited.
- `CURRICULUM.md` → `docs/reference/curriculum.md` (`git mv`), its 245 notebook links
  repointed `notebooks/` → `../../notebooks/`, and all five citers updated in the same
  commit: `generate_docs.py`, `curriculum.py`'s docstring, `Makefile`'s `docs` target,
  `README.md`'s reusable-core table. Verified with `difflib` that the moved file is
  IDENTICAL to `git show HEAD:CURRICULUM.md` modulo the repointing and the banner.
- `generate_docs.py` — the two defects `900` explicitly handed to this sweep, both real:
  `main()` wrote `docs/gap-analysis.md` while the tracked file was
  `docs/explanation/gap-analysis.md` (so `make docs` produced an orphan and left the real
  one stale), and neither generator emitted a banner (so regenerating a compliant doc
  demoted it). Both fixed. Proof it is now in sync: regenerating reproduces
  `gap-analysis.md` byte-identically apart from the banner date.
- `scripts/check-docs-structure.mjs` — NEW. R1/R2/R3 restated in its own header so it is
  self-contained for a repo that cannot read rosetta.
- `.chief/verify.sh` + `.github/workflows/ci.yml` — the guard wired in. **CI carried
  neither doc gate before this**: `check-doc-links.mjs` blocked regressions in verify.sh
  while a PR that never ran verify.sh merged them anyway.
- `CLAUDE.md` — Gates section now documents both doc gates and the wall/ratchet split.

**Verification**
- `node scripts/check-docs-structure.mjs` → `docs-structure: OK`, exit 0. **Proved it
  fires**, which matters more: an injected undeclared `docs/planning/x.md` → 3 violations,
  exit 1; a banner-less `docs/reference/y.md` → 2 violations, exit 1.
- `node scripts/check-doc-links.mjs` → `dead references 0 (0 distinct targets)`, before
  and after.
- `chief verify` → `doc-links: 0 dead vs 0 on main` · `nbgrader validate: 117 notebooks,
  0 failures` · `coverage HOLDS` (117/245, 14/14 domains) · `1205 passed, 1 skipped` ·
  `verify: PASS`, exit 0. Identical test counts to `900`'s — nothing was skipped or
  adjusted to get there.

**Deliberately NOT done, and said out loud rather than left silent**
- The Tier-1 root-file rule is **not** enforced by the new guard. `technologies.md` is
  still at the root pending US-3's archival, and a gate that is red the day it lands is a
  gate someone switches off. The guard's header says so; US-3 should add the rule when it
  archives that file.
- The new verify.sh step was not exercised by `chief verify` (see Codebase Patterns —
  `chief verify` runs the main checkout's script). It was run directly instead.

**Learnings for future iterations**
- US-2 has real material already located: `curriculum.py`'s module docstring says "the 11
  hand-written study domains" and "Domains 1-10" while the live figure is **14 domains /
  245 notebooks** — a stale count in exactly the class US-2 names. Grep for other counts
  before trusting any of them.
- `docs/reference/curriculum.md` and `docs/explanation/gap-analysis.md` are GENERATED.
  Never hand-edit them; run `make docs` and commit the output.
- Don't touch `README.md`'s gate-coverage sentence casually — `praxis.gatefloor.claim()`
  parses it, and `verify.sh` fails the merge if it disagrees with
  `notebooks/coverage-floor.json`.
- Editing `README.md` or anything under `scripts/` pulls in the full python job (~7 min of
  pytest). Budget for it.

---

## US-2 — Content that describes a tree that moved  (commit 86c175e)

**Method.** Read every tracked `.md` outside `notebooks/`/`tasks/` against the tree, then
machine-checked what could be: every backticked path resolved, every ``symbol()`` cited in
prose grepped for a definition, every route in README's API table matched against
`launcher/app.py`'s 26 decorators, and every count re-measured by running the command that
produces it. 10 files carried drift.

**Corrections, in the portfolio's corrections-of-record style** — `**[CORRECTED
2026-09-03 — what was wrong]**`, the same inline form `ROADMAP.md` uses. A silently fixed
doc teaches nobody why it drifted, and every one of these drifted the same way: a number or
a list restated in a second place.

*Counts the batch moved and the docs did not*
- `docs/reference/chief-powered-construction.md` — "**221 ungated notebooks**", present
  tense. `chief/87` ran; the backlog is 128. The page now states **no count** and defers to
  `python3 -m praxis.coverage`. Its console sample's trailing line updated 221→128 (the two
  per-unit rows above it are still exact — verified against a live run).
- `docs/reference/gate-authority.md` — "the 24 gated seed notebooks carry 168 graded cells"
  read as the present library; it was the size of the leak when the filter landed. Dated.
- `docs/explanation/jupyterquiz-assessment.md` — "as it ships: 24 checksets, 144 checks".
  **Re-measured rather than assumed**, because the DECLINE rests on the *proportions*:
  `python3 -m praxis.gateaudit` → 117 gates / 702 checks / 0 flagged, 354 `choice` + 236
  `short` + 112 `code` = 50/34/16% against the 50/33/17% the argument was made from. The
  conclusion stands and the decision's own figures are left at the sizes it was taken on.

*Claims the code no longer supports*
- `docs/explanation/notebook-rubric.md` — progress "default `.praxis/`". That default died
  with storage backends (`progress.progress_dir()` is a one-line delegate to
  `praxis.storage`). It now names no path and points at `docs/reference/storage.md`.
- `README.md` — "**Three** backends ship", listing three. `webdav` shipped as the fourth
  through `register_backend()`; `docs/reference/storage.md` has had four all along.
- `README.md` — the launcher API table calls itself "the whole product over HTTP" and was
  missing **seven** JD-suggestion routes and `/healthz`.
- `README.md` + `docs/reference/storage.md` — `POST /api/storage/sync` documented as the
  *cloud* mirror's; `webdav` registers a `_SYNC` too.
- `curriculum.py` docstring — "the 11 hand-written study domains" (measured:
  `len(DOMAINS)==14`, 180 manifest topics, 245 notebooks), "Domains 1-10" (01–10 + 12–15,
  06 reserved), and `notebooks/subjects/` for generated subjects. That last path was stale
  in three more docstrings: `praxis/curriculum_gen.py`, `praxis/construct.py`'s CLI
  example, `scaffold_notebooks.py`.

*Duplication reduced to one home*
- The merge gate's list of checks was stated in `README.md`, `docs/reference/packaging.md`
  **and** `.github/workflows/ci.yml`'s own header — all three as "**three** checks". It has
  been six since the doc gates landed. README's **The gate** section now holds the full
  table (check × scope) and packaging.md, ci.yml and `CLAUDE.md` point at it.

**Verified current and deliberately left alone** — `docs/reference/jupyterlite.md`'s
101/144/245 (`python3 -m praxis.lite plan` prints exactly that), `docs/explanation/
gating-backfill-cost.md` (117/245 · 702 checks · 128 remaining, all re-measured), the two
generated docs (`python3 generate_docs.py` → zero-byte diff), and
`docs/explanation/dead-code-inventory.md`'s symbols, which are *named as removed*.

**Out of scope, so reported rather than fixed:** `ROADMAP.md` says "the three storage
backends" and is wrong the same way README was. The tasklist puts roadmaps out of scope;
US-3 should carry it in its "what was left alone" report.

**What this did NOT verify** — link resolution and structure are machine-checked; **prose
correctness is not**. Every count, path, module name, command and route was executed or
grepped. That the *remaining* prose is true is not something this method establishes, and
US-3's honesty criterion should say so.

**Gate** — `chief verify` → `doc-links: 0 dead vs 0 on main` · `docs-structure: OK` ·
`nbgrader validate: 117 notebooks, 0 failures` · `coverage HOLDS` (117/245, 14 of 14) ·
`1205 passed, 1 skipped` · `verify: PASS`, exit 0. Identical counts to US-1's.

**Learnings for future iterations**
- **A count in prose is a second implementation.** Every stale figure here was a number
  restated somewhere a command already prints. The fix that holds is deleting the restated
  copy and naming the command, not updating the number.
- **`.github/workflows/ci.yml`'s header comment is documentation and it drifts.** US-1
  added two CI jobs and left the header saying "same three checks". When you add a gate,
  the file's own comment is part of "change one and change the other".
- **The corrections style this portfolio uses is inline:** `**[CORRECTED YYYY-MM-DD — …]**`
  (see `ROADMAP.md:80`). Keep the marker's inner text free of `**bold**` — nested bold does
  not render.
- Grep `launcher/app.py` for `@app.(get|post|put|patch|delete)` before trusting README's
  route table; it had drifted by seven routes.
- `python3 -m praxis.tasklist` / `praxis.lite plan` / `praxis.gateaudit` / `praxis.coverage`
  all run offline with no key, so any doc figure they produce is cheap to re-verify. Do it
  rather than believing the page.
