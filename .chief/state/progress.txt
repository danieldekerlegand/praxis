# Chief Progress — 83-seed-library-refresh
Started: Fri Aug 21 19:06:42 CDT 2026
---

## Codebase Patterns

- Promotion must remain a policy layer over `construct_topic`; the constructor and checks grader are the only accepted gates before documentation records a seed as covered.

## Iteration US-3

Implemented `praxis.promote` with a repository-only promotion path for recommended topics. It requires the constructor result, `gate_failures`, the complete badge, and a valid checkset before moving a topic from gap-analysis §2 into its domain's §1 coverage section. Added `docs/explanation/domain-addition-policy.md`, reserving domain 06 and validating `DOMAINS` against numbered directories, plus focused promotion tests.

Verification observed: `python3 -m pytest -q tests/` — 917 passed, 1 skipped; promotion/refresh tests — 6 passed.

Learnings for future iterations: the manifest includes a filesystem-backed legacy domain, so numbering validation must compare only top-level numbered directories while allowing nested legacy notebooks.
