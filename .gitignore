# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Jupyter / launcher runtime
.jupyter/
*.DS_Store

# Subjects a user defines in the app — their curricula and notebooks, not the
# seed library. notebooks/<NN-domain>/ stays tracked.
notebooks/subjects/

# Tauri shell build output
ui/node_modules/
ui/dist/
src-tauri/target/
src-tauri/gen/
# Chief runner state (only state/ is transient; config, verify.sh, agent-context.md are tracked)
.chief/state/
